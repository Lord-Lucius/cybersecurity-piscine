//! SQL engine fingerprinting. Phase 5.
//!
//! `fingerprint.rs`: the `SqlEngine` enum and `fingerprint()`, which decides the
//! backend from error signatures or engine-specific probes. Re-export once filled.

pub mod fingerprint;
